# Operations AGENTS

Focus: shifts, schedules, attendance, posts, contracts, and operational compliance.

Use real DocType names in commands, for example:
```bash
bench --site <site> run-tests --doctype "Operations Shift"
```
Do not list tests unless they exist in the repo.
