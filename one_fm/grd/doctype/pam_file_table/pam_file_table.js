frappe.ui.form.on("PAM File Table", function(frm) {
        
    // var d = locals[cdt][cdn];
    // console.log("check total");
    //     //var result = frm.doc.total;
    //     frappe.model.set_value(cdt, cdn, "total", d.inside + d.outside);
        
    });
    // frappe.ui.form.on("PAM File Table", "outside", function(frm, cdt, cdn) {
    //     var sum = 0;
    //     // var d = locals[cdt][cdn];
    //     // frappe.model.set_value(cdt, cdn, d.total, d.inside+d.outside);
    //     $.each(frm.doc.pam_file_table, function(i, d){
    //         sum = flt(d.inside) + flt(d.outside);
    //     });
    //     frm.set_value("total", sum);   
    // });
       