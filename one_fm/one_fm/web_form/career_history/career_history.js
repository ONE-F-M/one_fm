frappe.ready(function() {
	// bind events here
  // frappe.web_form.fields_dict["tablename"].grid.get_field("fieldname").get_query = function(){
	// 		return {
	// 			query: "method_path",
	// 		};
	// 	}

  // frappe.web_form.fields_dict['career_history_company'].grid.get_data('company_name')
})
